server:
  port: 33381
  tomcat:
    threads:
      max: 400
      min-spare: 75
    accept-count: 750
    max-connections: 7500

spring:
  application:
    name: waibao-seckill

  cloud:
    nacos:
      server-addr: 10.61.20.211:38848
      discovery:
        namespace: waibao
      username: nacos
      password: nacos

      config:
        namespace: waibao
        extension-configs: application.yml,${spring.application.name}.yml